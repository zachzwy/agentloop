# Agent notes for the demo project

- Contribution rule: every new `.js` file must start with the exact header line
  `// @project: demo` as its first line.
- Format the project with `npm run format` (a self-contained script; no deps).
- Source lives in `src/`; helpers in `lib/`; build artifacts in `build/`.
