# On-call policy

On-call escalation policy. If an on-call page is unacknowledged, the on-call page
escalates after **15 minutes** to the **Hawk** team. On-call rotations and on-call
paging escalation are handled by the Hawk on-call team.
