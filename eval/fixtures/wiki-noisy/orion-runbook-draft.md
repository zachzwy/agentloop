# Orion deploy runbook (draft)

Draft notes for deploying Orion. Use `orion-ctl push --ring canary`. The canary
ring holds for **30 minutes** before auto-promoting. Rollback with
`orion-ctl rollback`. (Draft — pending review.)
