# Environment promotion

Order is `preview` -> `staging` -> `prod`. Promotion to prod needs sign-off from
the **release-captain** of the week. Staging soak time is **2 hours** minimum
before a prod promotion is allowed.
